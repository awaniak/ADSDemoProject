spring:
    datasource:
        password: password
        url: jdbc:mysql://localhost:3306/db
        username: user_test
    flyway:
        password: password
        schemas: db_schema
        url: jdbc:mysql://localhost:3306/db
        user: user_test
    jpa:
        database-platform: org.hibernate.dialect.MySQL8Dialect
        hibernate:
            ddl-auto: update
